
export class AddressModelService {
    /*
        wallet uuid flag
        if wallet is new wallet and don't storage, _uuid is undefined!
    */
    id: string

    tag: string
    address: string
}
